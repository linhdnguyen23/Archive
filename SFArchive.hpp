/**
* SFData
*
* Description : Performs typical archival tasks.
**/

#include <map>
#include <string>
#include "SFData.hpp"

class sfarchive {
public:

private:
  std::map<std::string, SFData> containedData;
  
};
